{
  "name": "time-allocated-to-issue",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "start": "concurrently \"npm:server\" \"npm:client\"",
    "server": "node server/index.js",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
